import Route from '@ember/routing/route';

/**
    Provides Ember route for default view (home page).

    @module HomeRoute
    @extends Ember.Route
 */
export default class HomeRoute extends Route {}
